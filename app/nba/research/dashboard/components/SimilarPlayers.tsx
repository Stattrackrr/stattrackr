'use client';

/** Stub component - SimilarPlayers UI can be implemented later. */
export function SimilarPlayers({
  playerId,
  opponent,
  statType,
  isDark,
  shouldFetch,
}: {
  playerId: string | number;
  opponent?: string;
  statType?: string;
  isDark?: boolean;
  shouldFetch?: boolean;
}) {
  return null;
}
